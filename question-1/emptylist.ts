let usernames:string[]=[]

    if(usernames.length===0){

    console.log("We need some users")
    }
    else{//In this code, the check for an empty array is done outside the loop, and if there are users, it will iterate through the array and print the message for each user. If the array is empty, it will print the message indicating the need for users.

        usernames.forEach(element => {
        console.log("great you are user")
         } )  } 
